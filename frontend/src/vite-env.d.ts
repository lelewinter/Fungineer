/// <reference types="vite/client" />

/** Injected at build time by Vite `define` (see vite.config.ts).
 *  Short commit SHA + build date, e.g. "a1b2c3d · 2026-06-04". */
declare const __BUILD_ID__: string;

# Base URL of the Fungineer backend API.
# Leave EMPTY to disable remote save/load (game falls back to localStorage only).
#
# Local dev:
#   VITE_API_URL=http://localhost:8000
# Production (point at your Railway service):
#   VITE_API_URL=https://fungineer-api.up.railway.app
VITE_API_URL=
